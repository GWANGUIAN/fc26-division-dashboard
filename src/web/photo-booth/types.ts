export type PhotoBoothState = {
  schemaVersion: 1;
  selectedStreamerId?: string;
  /** Whether the 우왁굳 congratulations cutout shows on the stage. Defaults to true when unset. */
  directorVisible?: boolean;
};
