# 잔디동 월드 — 기획 문서 인덱스 & 세션 핸드오프

좌측 상단 플로팅 버튼 **"잔디동 월드 구경하기"** → 전체 화면 캔버스 **2D 도트 RPG**. 잔디동 멤버 11명 중 1명을 골라 마을을 돌아다니며 NPC와 대화하고 미션(기존 3D 카드·미니게임 + 월드 내 미션)을 풀어 **시들어가는 황금 잔디**를 살리는 게임이다. 엔딩 후에도 오락실·일일 미션·수집·카드 도감으로 계속 들어올 이유를 준다.

**현재 상태: S0~S5 완료, S6 코드 폴리시·릴리스 점검 완료(2026-09-19).** 오디오 연결·청크 캐시 상한·reduced-motion·크레딧/공지·문서가 실제 코드와 일치한다. 단, 배포 전 외부 오디오의 원본 출처·라이선스를 확인해 [07](07-audio.md)에 기록하고, 누락된 B14 `region-weed`를 제공하거나 지금의 필드 BGM 폴백을 승인해야 한다.

## 문서 지도

| 문서 | 내용 | 언제 보나 |
| --- | --- | --- |
| [01-concept-and-architecture.md](01-concept-and-architecture.md) | 규격(640×360, 타일 32px, 프레임 48×64), 화면 흐름, 폴더 구조, 렌더 레이어, 데이터 스키마, 로딩, 오디오 설계, **기존 코드 통합 지점** | 코딩 세션 시작 시 항상 |
| [02-story-and-missions.md](02-story-and-missions.md) | 세계관, 3막 스토리, 캐스트 20명, 튜토리얼, 마커 규칙, **미션 11개 상세 + 대사 초안**, 결전·엔딩, 엔딩 후 콘텐츠 | 대사·미션 구현, **대사 검수** |
| [03-map-design.md](03-map-design.md) | 80×60 맵 존/좌표표, 건물 17·실내 19, 길, NPC 배치, 미션 오브젝트, 수집품 20, 맵 JSON 스키마 | 맵 제작·충돌·배치 |
| [04-art-characters.md](04-art-characters.md) | 스타일 바이블/STYLE_BLOCK, 4단계 캐릭터 워크플로, 20명 프롬프트·파일명, QA | **이미지 생성(캐릭터)** |
| [05-art-world.md](05-art-world.md) | 지면 8·소품 11·건물 17·실내 19·러시·FX 프롬프트 | **이미지 생성(월드)** |
| [06-art-ui.md](06-art-ui.md) | 플로팅 버튼·키아트·9‑slice 프레임·아이콘 프롬프트 + CSS 규격 | **이미지 생성(UI)** |
| [07-audio.md](07-audio.md) | BGM 14·SFX 53·앰비언스 9, 저장 파일명, **한/영 검색 키워드**, 재사용 파일 | **오디오 수집** |
| [08-implementation-roadmap.md](08-implementation-roadmap.md) | 세션 S1~S6 계획, 아트 트랙 A1~A7, 변환 스크립트 사양, 테스트, 미해결 항목 | 세션 계획·진행 |
| [09-asset-checklist.md](09-asset-checklist.md) | 전 에셋 마스터 체크리스트(진행 추적) | 에셋 작업 후 체크 |
| [10-image-generation-runbook.md](10-image-generation-runbook.md) | **이미지 151장 실행 순서표**: #001부터 저장 이름·스레드·레퍼런스·독립형 프롬프트(스타일 문구 인라인). `tools/build-image-runbook.mjs`로 04~06에서 생성 | **이미지 생성할 때 이 문서만 보면 됨** |

## 한눈에 보는 수치

| 항목 | 값 |
| --- | --- |
| 화면 | 논리 640×360, 정수 배율, 타일 32px, 캐릭터 48×64, 실내 640×384 |
| 렌더 | Canvas 2D 직접 구현 + DOM UI(9‑slice), Phaser/Pixi 미사용 |
| 맵 | 80×60 타일(2560×1920px), 지구 8, 집 11, 클럽하우스·스타디움·편의점·카페·제초 공장, 실내 19 |
| 캐스트 | 20 = 멤버 11 + 우왁굳 + 오리지널 6 + 동물 2 |
| 미션 | 메인 11 정의(본인 제외 10개 → 조각 10개), 튜토리얼 3, 서브 6종, 결전 1, 엔딩 후 장기 1 |
| 기존 기능 연동 | 3D 카드 열람/변형, 합10·공튀기기·3D 프리킥·카드 짝맞추기 (+신규 잔디 러시) |
| 이미지 생성 | 151장(P0 106 / P1 44 / P2 1) |
| 오디오 | BGM 14 / 신규 SFX 53 / 앰비언스 9 (+기존 파일 재사용) |
| 이미지 도구 | ChatGPT gpt-image (레퍼런스 첨부, 투명 PNG) |
| 모바일 | ≤680px에서 버튼 숨김(터치 계획 없음) |

## 핸드오프 체크리스트 (세션 진행 상태)

각 세션이 끝날 때마다 갱신한다. 새 세션은 여기부터 읽는다.

### 기획
- [x] **S0 기획 문서 세트 작성** (2026-09-19)
- [x] 대사 검수본 반영(`data/dialogueData.ts`, S4)
- [ ] 미션 임계값/맵 좌표 확인(사용자 의견 반영, 필요 시 02·03 수정)

### 구현 (08 참고)
- [x] **S1** 기반 — 버튼·오버레이·로딩·루프·입력·충돌·저장 (2026-09-19, 배포 후 `?worldDebug` 확인 체크리스트는 아래 세션 로그 참고)
- [x] **S2** 월드 뼈대 — 맵/렌더/씬 전환/NPC/대화 UI/캐릭터 선택/가이드 (2026-09-19, 배포 후 `?worldDebug` 확인 체크리스트는 아래 세션 로그 참고)
- [x] **S3** 미션·진행 — 상태기계/마커/로그/HUD/기존 기능 연동/월드 미션 메커닉 (2026-09-19, 배포 후 `?worldDebug` 확인 체크리스트는 아래 세션 로그 참고)
- [x] **S4** 스토리 콘텐츠 — 전 대사/실내/결전/엔딩 (2026-09-19, 아래 확인 체크리스트 참고)
- [x] **S5** 엔딩 후 — 잔디 러시/일일/수집/도감/공장 후속 (2026-09-19, 아래 수동 확인 목록 참고)
- [~] **S6** 폴리시·릴리스 — 코드/자동 검증·크레딧·공지 완료(2026-09-19); 외부 오디오 출처·라이선스 확인과 B14 결정이 배포 게이트로 남음

### 아트/오디오 트랙 (사용자 작업, 09에서 세부 체크)
- [x] **A1~A5, A7 이미지**: 원본 151장 전부 생성됨 → 변환 완료(447 webp, [09 §10](09-asset-checklist.md#10-변환-결과와-qa-s1-2026-09-19)). 톤 승인은 사용자가 배포본에서 확인(재생성 후보는 09 §10의 QA 목록)
- [x] **A5(버튼)** 플로팅 버튼 3장 — S1에서 사용 중
- [~] **A6** 오디오: BGM 13/14(`region-weed`만 없음)·SFX 53/53·앰비언스 9/9 수집·파일 검사·연결 완료(2026-09-19). 다운로드 출처/라이선스 메타데이터가 저장소에 없어 외부 배포 전 07에 기록 필요

### 세션 로그
| 날짜 | 세션 | 한 일 | 다음 |
| --- | --- | --- | --- |
| 2026-09-19 | S0 | 저장소 탐색(스택·오버레이·미니게임·3D 카드·멤버 데이터), 사용자 결정 반영, 문서 01~09 작성 | A1 스타일 테스트 시작 + S1 구현 |
| 2026-09-19 | S1 | 08 S1 전체 구현 + 변환 스크립트 전 카테고리 완성·원본 151장 변환. 미해결 #1(음악)·#2(버튼 겹침)·#4(키)를 사용자와 정해 08 §5에 기록, 원본 PNG git 추적 해제. 문서 01·06·08·09를 실제에 맞게 수정. `pnpm typecheck` + 377 tests 통과. 아래 확인 체크리스트 참고 | **S2** 월드 뼈대 + 사용자: 배포 후 S1 체크리스트 확인, QA 경고 캐릭터/프레임 재생성 여부 결정 |
| 2026-09-19 | S2 | 08 S2 전체 구현: 맵 생성 스크립트+`overworld.json`+실내 19곳 JSON, 지면 청크·경계 블렌딩·복원 크로스페이드 훅, y-sort/above, 씬 전환(실내 지연 로드), NPC AI·상호작용·임시 대사 상자, 캐릭터 선택·프롤로그·코치마크·지구 토스트, 오디오 뼈대, 이어하기 조건(`prologue-done`), `?worldDebug` 확장. 03/01/02/06/08/09 문서를 실제에 맞게 수정. `pnpm typecheck` + 테스트 통과(맵 도달성·헤드리스 엔진 스모크 포함). 아래 확인 체크리스트 참고 | **S3** 미션·진행 + 사용자: 배포 후 S2 체크리스트 확인, 실내 충돌/조사 좌표 보정, 프롤로그 임시 문구 검수 |
| 2026-09-19 | S3 | 08 S3 전체 구현: 미션 상태기계+판정+조건식+지구별 복원(순수, 테스트), 미션 정의(튜토리얼 3·메인 11·서브 2), 임시 대화 조립, 마커·HUD·미션 로그·일시정지 메뉴, 미니게임 4종 `onRoundEnd`·`TotyCardPopup` `onView` 연동(월드 안 모달), 오락실 기계·카드 수납장 연결, 월드 메커닉(줍기·택배·콘 코스·킥 볼·소문), 맵에 골대·펜스·우편함·콘 코스·랜턴/잔디 자리 추가, `?worldDebug` 미션 도구. 01/02/03/06/07/08/09를 실제에 맞게 수정(Esc는 이제 메뉴, 카드 판정은 `onView`만, 콘 코스 재설계 등). `pnpm typecheck` + 테스트 통과. 아래 확인 체크리스트 참고 | **S4** 스토리 콘텐츠 + 사용자: 배포 후 S3 체크리스트 확인, 콘 코스/택배 시간 튜닝, 대사 검수 |
| 2026-09-19 | S4 | 검수본 전 대사를 `dialogueData.ts`로 교체하고 프롤로그·실내 조사·`first/pre/idle/post/home/rumor/cheer` 상태를 연결. 우왁굳 3/6/9·결전 제안, 조건부 스타디움/제초동·트로피룸 액자, 3연전·관중석·개화·기존 단체샷·크레딧·엔딩 플래그, 지구 색조/파티클/앰비언스 훅을 구현. 문서 불일치와 엔딩 뒤 진행도 대사 재노출 버그를 고침. `pnpm typecheck` + 650 tests 통과 | **S5** 엔딩 후 반복 콘텐츠 + 사용자: 아래 S4 디버그 체크, 난이도·실내 좌표 확인 |
| 2026-09-19 | S5 | 잔디 러시 순수 엔진·월드 모달·5번 기계, 5종 최고 기록/랭크, KST 일일 3과제·1일 1스탬프·7/14/30 뱃지, 황금 공 20개 접촉 수집·도감, 카드 도감/히든 안내, 공장 테마/정원 후속과 코치 미션, 엔딩 타이틀 색상 전환. 시작 650개 → 최종 670개 테스트 및 타입 검사 통과. S4 대사·게이트와 앰비언스 선택은 유지 | **S6** 폴리시 + 아래 S5 수동 확인 |
| 2026-09-19 | S6 | BGM 14·SFX 53·선택 앰비언스 8(파일 9, water 미선택) 매핑/파일 폴백 테스트, 빠져 있던 문종·동물·벽 충돌·제초동·러시 SFX 연결, 크레딧 메뉴·공지, 청크 캐시 상한(40개/약 40MiB) 테스트와 에셋 총량 점검을 완료. reduced-motion·Esc/포커스/모바일 규칙은 코드와 기존 회귀 테스트로 보존. `pnpm typecheck` 및 **674개 테스트** 통과 | **배포 전** B14 `region-weed` 결정 및 오디오별 제작자·URL·라이선스 기록(07) |

**S5 배포 후 `?worldDebug` 확인 체크리스트** (이번 세션에서는 브라우저 검증하지 않음)
1. 오락실 `interior:arcade` 5번 기계: 시작 → Space/↑ 점프·↓ 슬라이드 → 충돌 결과 → 재도전. Esc는 모달만 닫고 월드는 움직이지 않는지. 재접속 후 도감의 최고 거리·랭크가 유지되는지.
2. 엔딩 플래그를 설정하고 광장 게시판 (44,25): 날짜·3개 과제 확인 → 달성 → 스탬프 수령 → 같은 날 재수령 불가. KST 자정에 열린 게시판이 갱신되고 이전 과제 달성으로 새 날짜 보상을 받을 수 없는지.
3. 광장 도감 (46,25) / 오락실 기록판 (10,8): 황금 공 20개 위치·카드 11명·뱃지·게임 랭크 표시. 공은 접촉 시 한 번만 획득, 엔딩 전 제초동 3개 숨김. 재접속 후 수집 상태 유지.
4. 감독실 카드 팝업: 공개/테마 변경 콜백만 기록, 멤버 전환·우왁굳 히든 카드 선택 정상. 11명 공개 후 `m-91-cards` 보고. 외부 대시보드 카드 열람만으로 월드 미션이 채워지지 않는지.
5. 공장 (8,6)에서 공장 테마 러시, 정원사 의뢰 → 할아버지/제초왕 조언 → 보고 → 정원 조사 문구. 제초왕의 일일 훈련 보고, 꼬마의 아무 황금 공 5개, 상점 주인의 러시 1000m 의뢰 확인.
6. S4 `post[]`/`home[]`, 제초동 게이트·공장 입장, 기존 결전·엔딩이 그대로인지. 엔딩 세이브로 재진입한 타이틀의 초록 색조, 새 패널 Tab 포커스·스크롤·Esc·월드 입력 차단 확인.

**S1 배포 후 `?worldDebug` 확인 체크리스트** (사이트 주소 뒤에 `?worldDebug`)
1. 좌상단 버튼: 첫 방문엔 글로우+말풍선("잔디동 마을이 열렸어요!"), 클릭하면 사라지고 다시 안 나옴. 판 이미지 + 글자가 겹치지 않고, hover 시 밝아짐. 창 폭 1000~1900px에서 브랜드·공지 위젯이 버튼에 안 가려짐. 아래로 스크롤하면 원형 아이콘으로 축소되고 검색바가 안 가려짐. 폭 680px 이하에선 버튼 없음.
2. 로딩: 진행률 바가 차오르고 팁 문구가 바뀜, 최소 0.6초 표시 후 타이틀. 타이틀: 배경·로고·버튼 판 이미지, ↑↓ Enter로 선택. 저장이 있으면 "이어하기" 노출, "새로 시작"은 한 번 더 눌러야 지워짐.
3. 플레이: 재닌(실제 스프라이트)이 방향키/WASD로 이동, Shift로 달리기, 방향별 idle/걷기 프레임, 좌향은 반전. 한글 IME를 켠 채로도 WASD 이동. 방향키/Space로 뒤 페이지가 스크롤되지 않음.
4. 충돌: 초록 상자(발 충돌 박스)가 빨간 상자(나무 밑동·바위·벤치·울타리)에 막혀 슬라이드하고 모서리에 걸리지 않음. 노란 상자(이동 가능 영역) 밖으로 못 나감. 맵 가장자리에서 카메라가 멈춤(1280×960 맵). 콘솔에 에러 없음.
5. 선명도: 화면 배율이 `scale Nx device px`로 표시되고 도트가 번지지 않음(창 크기·브라우저 확대 변경 후에도). FPS ≈ 60.
6. 종료/재진입: Esc, 우상단 X, 타이틀의 "나가기" 모두 닫힘(*S3부터 플레이 중 Esc는 메뉴*). 다시 열면 로딩부터 정상이고, "이어하기"로 마지막 위치에서 시작. 월드를 여는 동안 YouTube 음악이 멈췄다가 닫으면 (재생 중이었다면) 다시 재생.
7. 기존 기능(미니게임·카드·테마 토글)은 평소와 동일해야 함.

**S2 배포 후 `?worldDebug` 확인 체크리스트** (사이트 주소 뒤에 `?worldDebug`. 좌측 패널이 디버그 도구, 빨강=충돌·청록=문·보라=조사·주황=NPC·연두=상호작용 프로브)
1. **타이틀 → 캐릭터 선택**: "새로 시작"에서 4×3 카드 11명이 뜨고 방향키/마우스로 움직인다. 고른 카드는 금색 프레임+위에서 통통 뛰는 공, 왼쪽에 큰 스탠딩·이름·포지션·집. Enter/"선택"으로 확정, Esc는 타이틀로(이때 기존 저장은 그대로). 네 가지 카드 프레임의 높이 차이가 눌려 보이지 않는지.
2. **프롤로그**: 임시 4줄이 타자기로 나오고 E/Enter/클릭으로 넘어감, Esc는 건너뜀. 끝나면 로딩 후 **내 집 실내**에서 위를 보고 서 있고 상단에 코치마크 "1/3".
3. **실내(19곳)**: 패널의 씬 목록으로 하나씩 들어가 그림이 화면에 맞는지(위아래 12px 잘림), 빨간 충돌 상자가 가구·벽과 맞는지, 캐릭터가 가구 위를 걷지 않는지, 문 아래로 나가면 페이드 후 집 앞마당. 어긋난 곳은 캔버스를 클릭해 뜨는 **px/tile 좌표를 복사**해 `interiors/<id>.json`을 고치면 된다. 조사 영역(보라)에 다가가 E로 문구가 뜨는지.
4. **마을**: 지면이 실제 텍스처로 깔리고 지구가 바뀔 때 하단에 토스트(지구 이름+색 점). 지구 경계(예: 중앙↔봄 정원, 중앙↔서리)가 디더로 부드럽게 섞이고 길·광장은 선명한지. 기본은 **시든 색**(조각 0개)이고 패널의 "색 복원 미리보기"를 100%로 올리면 초록·나무가 크로스페이드된다(시든 이미지가 없는 물·공업·제초 지면은 안 변함).
5. **소품·건물**: 나무 캐노피가 아래에서 올려다볼 때 머리를 덮고 줄기 뒤로 가면 가려진다, 건물 앞을 지나가면 건물 앞뒤 정렬이 맞다, 건물 벽은 전부 막히고 문 칸만 들어가진다(건물 정면을 따라 걷기만 해서는 안 들어감). 스타디움 문은 **남쪽 정면**.
6. **NPC**: 광장 잔디 할아버지에게 다가가면 머리 위 `E`(전방 28px 안), E로 임시 대사+선택지(↑↓/Enter), 대사 중 이동 정지·Esc로 닫기, 대사가 시작되면 NPC가 돌아본다. 꼬마·잔디냥·공돌이는 정해진 구역에서 배회하고 막히면 방향을 바꾼다. 멤버들은 집 앞마당에 서 있고 **내가 고른 멤버의 자리만 비어 있다**. 걷다가 캐릭터가 NPC를 뚫지 않는지.
7. **집/건물 입장·퇴장**: 문 칸 안으로 걸으면 페이드→실내→(아래 문)→페이드→집 앞. 클럽하우스 로비의 왼쪽 문(감독실)·오른쪽 문(트로피룸)·뒤 계단(오락실)과 편의점·스타디움. 실내 이미지가 처음 들어갈 때 잠깐 검은 화면이 길어지는지(지연 로드).
8. **코치마크**: C1 걷기 3타일 → C2(할아버지 위에 황금 화살표, 화면 밖이면 가장자리 화살표) 대화 종료 → C3 J 키(S3부터 미션 로그가 실제로 열림). 마치면 다시 안 뜨고, 도중에 Esc로 건너뛸 수 있다. 코치마크가 떠 있어도 이동은 된다.
9. **Esc 순서**: 대사 → 코치마크 → (없으면) 월드 종료. 프롤로그·선택 화면에서는 각각 건너뛰기·타이틀로. *(S3부터: 플레이 중 아무것도 없을 때 Esc는 일시정지 메뉴를 연다. 월드 종료는 메뉴나 X.)*
10. **이어하기**: 프롤로그를 마친 뒤 닫았다 다시 열면 "이어하기"가 나오고 마지막 위치(실내면 실내)에서 시작. S1 때 만든 저장이 남은 브라우저에서는 "이어하기"가 안 나온다.
11. **소리·콘솔**: BGM/효과음 파일이 없으니 무음이 정상이고 콘솔에 에러가 없어야 한다(있다면 알려 주세요).
12. 기존 기능(미니게임·카드·테마 토글)은 평소와 동일.

**S3 배포 후 `?worldDebug` 확인 체크리스트** (사이트 주소 뒤에 `?worldDebug`. 좌측 패널 **"미션·진행"** 도구로 조각·플래그·미션 상태를 바꿔 볼 수 있고, 골(노랑)·체크포인트(초록)·줍는 것/공(하늘)·콘(주황) 오브젝트가 격자 위에 id와 함께 그려진다)
1. **HUD**: 좌상단에 조각 게이지(10칸)와 그 아래 트래커. 게이지의 채워진 칸이 프레임의 둥근 칸 위에 맞게 놓이는지(어긋나면 `world-mission.css`의 `--gauge-x/--gauge-pitch/--gauge-pip`). 트래커 글자가 잘리지 않는지, 보고 가능이면 깜빡이는지.
2. **튜토리얼(디버그 없이 처음부터)**: 새로 시작 → 프롤로그 → 집에서 나와 광장 할아버지에게 말 걸기 → 대화가 끝나면 "미션 완료: 마을 인사" 토스트, 트래커가 "감독님의 인사"로 바뀜 → 클럽하우스 로비 왼쪽 문의 감독실에서 우왁굳 머리 위 파란 `?` → 말 걸어 "받는다" → 머리 위가 회색 `…` → 카드 수납장(E)에서 카드 팝업이 뜨고 뒤집기가 끝나면 "목표 달성" 토스트 + 우왁굳 머리 위 **금색 `!`** → 보고하면 완료 + 메인 미션 10개(내가 고른 멤버 것은 빠짐)가 열림.
3. **Esc 스택**: 미니게임/카드 팝업 위에서 Esc는 팝업만 닫고(월드는 그대로), 메뉴의 설정/새로 시작 페이지에서는 한 단계 뒤로, 메뉴·미션 로그·대사 순으로 닫히고, 아무것도 없을 때 Esc는 **메뉴를 연다**(월드 종료 아님 — "월드 나가기"나 우상단 X). 코치마크가 있으면 첫 Esc가 그걸 건너뜀.
4. **일시정지 메뉴**: 설정에서 음악/효과음 켜기·볼륨(←→)이 바로 반영되고 다시 열어도 유지, "가이드 다시 보기"가 코치마크 1/3을 다시 띄움, "새로 시작"은 한 번 더 확인 후 캐릭터 선택으로 가며 Esc로 타이틀에 돌아가면 "이어하기"가 남아 있음.
5. **미션 로그(J)**: 진행/완료 탭(←→), 항목 선택(↑↓)과 오른쪽 상세(목표·의뢰인·장소·진행·보상), E로 트래커 지정(★), J/Esc로 닫힘. 재닌으로 시작하면 "서리 카드 뒤집기"가 없고 조각은 항상 10개.
6. **메인 미션 하나씩**(패널의 "튜토리얼 건너뛰기" 후 각 NPC에게 가서 수락):
   - **문모모·핑구·한결·재닌**: 클럽하우스 로비 안쪽 계단 → 오락실. 1번 합 10(≥60점), 2번 공 튀기기(≥20), 3번 프리킥(≥4골), 4번 카드 짝 맞추기(≤22턴)를 월드 안 모달로 플레이해 임계값을 넘기면 "목표 달성". 모자라면 안 넘어감. **대시보드에서 한 게임은 무관.** 모달을 닫으면 월드 음악이 돌아오고 Esc는 모달만 닫음.
   - **해파린**: 수락 전에는 호수에 랜턴이 안 보이고 수락 후에 나타남. (57,36)·(66,41)·(74,31)에서 E로 줍기 → "1/3 해파리 랜턴" 토스트, 다 모으면 `!`.
   - **쥬멩이**: 훈련장(북쪽 펜스 안, 출입구는 남·동쪽)에서 공 앞에 서서 E로 차기(바라보는 방향), 서쪽 골대 면에 닿으면 골. 수락 후 첫 킥이 60초 타이머(화면 위 가운데)를 시작하고 5골이면 달성, 시간 종료면 "다시 차서 재도전". 골 뒤 공이 제자리로 돌아옴.
   - **리냐·뽀린걸**: 감독실 카드 수납장 → 팝업 왼쪽 위 "다른 선수 카드 보기"로 그 멤버 선택 → 테마 선택에서 "조카의 스케치북"(리냐) / "90년대 고전 도트"(뽀린걸)를 고르면 달성(처음부터 그 테마로 나와도 인정).
   - **하치**: 수락 후 꼬마(광장)·편의점 사장(편의점 실내)·잔디 할아버지에게 각각 말을 걸면 소문 대사, 3명이 끝나면 `!`.
   - **빙밍**: 수락하면 "택배 3개" 토스트와 90초 타이머. 서쪽 골목 (14,38)·동쪽 서리 길 (69,27)·북쪽 룬 탑 앞 (68,13)의 우편함 앞에서 E → 배달 완료(우편함마다 자기 택배). 시간 초과면 빙밍에게 다시 말을 걸어 "다시 받는다".
   - **다시바**: 남서 콘 코스(4~18, 54~58). 시작 게이트를 지나면 타이머 시작, 콘(가운데 줄)을 **위/아래로 번갈아** 지나야 체크포인트가 카운트되고(직진하면 콘에 닿아 "+1초"가 뜨고 체크포인트는 안 지남), 도착 게이트에서 완료. 실패하면 시작 게이트를 다시 지나 재도전.
   - **서브**: 편의점 사장 "잔디 우유 배달" → 광장 할아버지에게 말 걸면 전달, 할아버지 "광장 잔디 물 주기" → 광장의 시든 잔디 5곳에서 E("물 주기").
7. **보상과 복원**: 완료하면 "미션 완료" → "잔디 조각 획득!(n/10)" → (5·10개면 뱃지) 토스트가 순서대로 뜨고 게이지가 채워지며, 맵 색이 서서히 살아난다 — 그 멤버 집 근처 지구가 더 빨리. 조각 10개에서도 **제초동 구역(남동)은 시든 채**(엔딩 전). 마지막에 "잔디 조각을 모두 모았어요!".
8. **패널 도구**: 조각 −/＋ 이 게이지·색에 바로 반영, "메인 전부 완료"로 색이 전체 복원(제초동 제외), "진행 초기화"로 처음 상태, 미션별 상태 지정("보고 가능"이면 `!`가 뜨고, "완료"는 보상이 한 번만 지급), 플래그 설정/해제(예: `main-open`), "타이머 취소"·"공 리셋". 색 복원 미리보기 슬라이더는 모든 지구를 한 값으로 덮어씀.
9. **집 안의 멤버**: 멤버 미션을 끝내면 그 멤버의 집 실내에도 본인이 나타남(내가 고른 멤버의 집은 그대로 비어 있음).
10. **이어하기와 기존 기능**: 진행 도중 닫았다 열면 미션·조각·수집품이 유지되고 진행 중이던 타이머만 사라짐(정상). 대시보드의 미니게임·카드·테마 토글은 평소와 동일, 콘솔에 에러 없음.

**S4 배포 후 `?worldDebug` 확인 체크리스트** (브라우저 일반 플레이는 하지 않고, 좌측 패널의 **결전 준비**·**스토리 초기화**·씬 이동·미션 상태 도구로 확인)
1. **대사 데이터**: 새 게임의 프롤로그 4줄·할아버지 튜토리얼·감독님 카드 수락/완료에 `(임시 대사)`가 없고, `?worldDebug`로 메인을 연 뒤 각 NPC의 제안→진행→보고→완료 후 idle을 확인한다. 미션 완료 뒤 해당 멤버 집에서는 `home[]`, `ending-seen` 뒤에는 `post[]`가 순환한다.
2. **진행도 이벤트**: 조각을 3/6/9로 맞춰 우왁굳에게 각각 한 번만 말하고, 10개에서 `finale-offer` 뒤 **스타디움 문 열림** 토스트와 문 조건을 확인한다. 엔딩 뒤에는 이전 beat가 다시 나오지 않는다.
3. **스타디움**: 결전 준비 → 스타디움으로 이동. 심판의 `?`, 제초왕, 플레이어 자신을 뺀 관중 10명과 E 응원 대사가 보인다. 심판과 제초왕 어느 쪽에서 시작해도 컷 1과 1라운드 선택지가 같다.
4. **3연전 판정**: 월드가 연 모달에서만 R1 합10 80점, R2 공튀기기 35회, R3 프리킥 5골이 순서대로 인정되는지 확인한다. 실패는 같은 라운드 재도전, 통과 라운드는 유지, 대시보드에서 한 게임은 결전에 영향을 주지 않는다.
5. **엔딩 순서/Esc**: R3 보고 → 코어 정지·개화 배너(개화 중 Esc 무시) → 제초왕/감독 대사 → 단체샷 → 크레딧 순서. 단체샷 Esc/X는 크레딧으로, 크레딧 E/Enter/Esc/클릭은 월드 복귀로 한 단계씩만 동작한다.
6. **엔딩 후 맵**: `ending-seen`, `area-weed-open`, `weed-restored` 뒤 제초동 게이트 바리케이드·충돌이 사라지고 공장 문·제초 요원·스타디움 옆 제초왕이 도달 가능하다. 제초동 색이 복원되고, 트로피룸 액자는 단체샷을 연다.
7. **실내·조사**: 패널 씬 목록으로 실내 19곳을 열어 출입문/충돌/조사 문구를 확인한다. 스타디움·트로피룸처럼 상태별 `when` 문구가 전환되는지도 본다.
8. **지구 연출·오디오**: 존을 옮길 때 tint/파티클이 바뀌고 reduced-motion에서 줄어드는지, 선택적 앰비언스 파일이 없을 때 콘솔 오류 없이 무음으로 넘어가는지 확인한다.

## 결정 로그

| 날짜 | 결정 | 이유 |
| --- | --- | --- |
| 2026-09-19 | Canvas 2D 직접 구현(엔진 라이브러리 미사용) | 사용자 요청(성능), 기존 캔버스 패턴 재사용, 번들 최소화 |
| 2026-09-19 | 이미지 도구 = ChatGPT gpt-image | 사용자 답변. 레퍼런스 첨부·투명 PNG 활용 |
| 2026-09-19 | 모바일은 월드 버튼 숨김 | 사용자 답변. 기존 미니게임 토글과 동일 |
| 2026-09-19 | 기존 4종 미니게임·3D 카드 팝업에 optional 콜백 prop만 추가 | 사용자 답변. 이번 판 점수로 정확 판정 |
| 2026-09-19 | 로딩 화면 + 에셋 프리로더(우선순위 그룹) | 사용자 추가 요청 |
| 2026-09-19 | 지면=심리스 텍스처 + 소품/건물=스프라이트 + 실내=통그림 | AI가 오토타일 세트를 못 맞추므로 정렬 요구 최소화 |
| 2026-09-19 | 시든 버전은 스크립트 자동 파생 | 생성 비용 절감 |
| 2026-09-19 | 캐릭터는 `group-photo/<id>.webp`를 레퍼런스로 도트화 | 실제 외형 일치 |
| 2026-09-19 | 마커: 파란 `?`=새 미션, 금색 `!`=조건 달성(보고 가능) | 사용자 요청("클리어한 미션 NPC 머리 위 느낌표") |
| 2026-09-19 | 엔딩은 기존 `GroupPhotoOverlay` 재사용 | 추가 아트 없이 기존 기능과 연결 |
| 2026-09-19 | 미션 판정은 월드에서 연 모달의 콜백만 인정 | 대시보드 토글 플레이와 분리 |
| 2026-09-19 | 서버 리더보드는 범위 밖 | Worker에 KV/D1 없음. 필요 시 별도 설계 |
| 2026-09-19 | 플로팅 버튼은 글자 없는 판 + CSS 텍스트 | AI 한글 렌더링 오류 방지 |
| 2026-09-19 | 캐릭터 정보는 `worldCast.ts` 하드코딩 | `roster.yaml`은 웹앱에 반영되지 않고 게스트는 코드 하드코딩이 프로젝트 규칙 |
| 2026-09-19 | 월드가 열리면 전역 YouTube 음악 일시정지, 닫으면 월드가 멈춘 경우만 복귀 | 사용자 답변(08 §5 #1). `musicControl.ts` + `MusicPlayer.tsx` 훅 |
| 2026-09-19 | 플로팅 버튼은 fixed + 여백 예약 + 스크롤 시 원형 축소 | 사용자 답변(08 §5 #2). 버튼이 브랜드·sticky 검색바를 가림 |
| 2026-09-19 | 키는 01 그대로, `code` 기반 window 캡처 | 사용자 답변(08 §5 #4). 대시보드 단축키 충돌 없음, 한글 IME 대응 |
| 2026-09-19 | `tmp/world-src/` 추적 해제(`git rm --cached`) | 사용자 답변. 이미 커밋된 원본이 .gitignore로 안 풀림 |
| 2026-09-19 | 변환은 격자 자르기 대신 연결 성분 추출 + 알파 240 하드닝, turn/walk는 시트별 스케일 | 생성 시트가 격자에 안 맞고 후광이 구워져 있음(08 §3) |
| 2026-09-19 | 사운드 설정은 세이브에서 분리(`fc26-world-settings-v1`만) | "새로 시작"이 사운드 설정을 지우지 않게. 01 §7 수정 |
| 2026-09-19 | 스테이지 배율은 디바이스 픽셀 기준 정수 | 125%/150% 화면에서도 도트 선명. 01 §2 수정 |
| 2026-09-19 | `above`는 소품별 px(`propDefs.aboveFrom`, 나무 40·큰 참나무 48·아치 48–64), 나머지는 y-sort만 | 사용자 답변(S2). 05 표에 높이가 없었음. 01 §5·03 §11 |
| 2026-09-19 | 건물은 보이는 스프라이트 전체가 벽, 문 칸만 통과 | 사용자 답변(S2). 지붕 뒤로 사라지는 구간 방지, 건물에 `above` 불필요. 03 §4·§11 수정 |
| 2026-09-19 | 스타디움 문을 북문 `(39–40,26)`에서 남쪽 정면 `(39–40,41)`로 이동 | 실제 이미지의 아치 게이트가 아래 중앙에 있음(문서 대신 문서 수정). 03 §4 |
| 2026-09-19 | 길·지구 표를 맵 위에 다시 그림(집을 관통하던 길, 지구 사이 빈 줄, 하치 집 안 우편함 등) | 표끼리 충돌. JSON이 진실. 03 §3·§5·§7 |
| 2026-09-19 | 소품 충돌은 05 표를 옮기되 보이는 그림보다 크면 그림 크기로 줄임(29개) | 파일 크기는 표와 같고 그림만 작음. 보이지 않는 벽 방지. 09 §11 |
| 2026-09-19 | 코치마크는 이동을 막지 않는 말풍선, Esc로 건너뜀 | C1이 걷기를 요구. 대사·프롤로그·선택 화면만 입력을 멈춤. 01 §2 |
| 2026-09-19 | "새로 시작"은 프롤로그를 마칠 때까지 기존 세이브를 지우지 않음, 이어하기는 `prologue-done`만 | 선택/프롤로그에서 나가도 세이브 보존, S1 샌드박스 세이브는 새 게임 취급. 01 §7 |
| 2026-09-19 | 인테리어 좌표·조사 포인트는 원래 문서 초안이 아니라 실제 이미지 기준으로 재작성 | 초안은 프롬프트의 배치를 짐작한 값이라 그림과 어긋남. 03 §9 |
| 2026-09-19 | (S3) 서브 미션은 S3 메커닉으로 되는 2개(`s-shop-milk`·`s-elder-water`)만, 나머지 3개는 S5 | 사용자 답변. 08 S3와 S5의 서브 범위가 어긋났음. 02 §10 |
| 2026-09-19 | (S3) 미션 대사는 미션 정보로 만든 임시 대사, 02의 초안은 검수 후 S4 | 사용자 답변. 실존 스트리머 대사는 검수 전에 코드에 넣지 않음. `state/npcDialogue.ts` |
| 2026-09-19 | (S3) 카드 미션은 월드 카드 팝업의 `onView` 콜백만 인정, `totyCardRevealedStore`는 판정에 안 씀 | 사용자 답변. 02 §12·08 S3의 "스토어 구독"을 고침(기존 결정 "월드에서 연 모달의 콜백만 인정"과 일치). 01 §10 |
| 2026-09-19 | (S3) Esc는 아무것도 열려 있지 않으면 일시정지 메뉴를 연다(월드 종료는 메뉴/X) | 08 §5 #3 스택에 메뉴·모달·로그 추가. 01 §2 |
| 2026-09-19 | (S3) 콘 코스는 가운데 줄 콘 + 위/아래 레인 체크포인트로 재설계 | 초안 배치는 가운데 줄 직진으로 콘 없이 통과됨. 03 §7 |
| 2026-09-19 | (S3) 세이브 스키마 버전은 저장 구조가 바뀔 때만 올림(v1 유지) | 08 §5 #11. 01 §7 |
| 2026-09-19 | (S3) 킥 볼은 NPC를 무시하고 골은 골대 면 접촉으로 판정 | 공돌이가 공 옆에서 시작해 킥을 막았음. 골대 소품이 단단해야 함. 01 §6 |

## 다음에 할 일

### 다음 버그 수정 세션 시작 지시문

아래 문단을 **사용자가 정리한 버그 목록 바로 앞에 붙인다**. 목록의 재현 절차·기대 결과·스크린샷/좌표는 그 다음에 그대로 적는다.

```text
S6 폴리시 점검까지 커밋된 잔디동 월드의 버그 수정 세션이다. 먼저 `docs/world/README.md`의 핸드오프 체크리스트·S1~S6 세션 로그·결정 로그와, `01-concept-and-architecture.md`의 입력/저장/오디오 규격, `08-implementation-roadmap.md`, `07-audio.md`, `09-asset-checklist.md`를 읽어라. 시작 즉시 `git status --short`, `pnpm typecheck && pnpm test`를 실행해 기준 상태를 기록한다.

아래 버그 목록에 있는 **재현 가능한 항목만** 조사·수정한다. S1~S6 기능을 재구현하거나 저장 키·스키마 버전·미션 ID·맵 객체 ID를 임의로 바꾸지 않는다. `WorldModals`가 월드 안에서 연 모달의 `onRoundEnd`/`onView`만 월드 미션 판정에 쓰며, 대시보드에서 한 플레이/카드 열람은 월드 판정에 섞지 않는다. Esc는 최상단 UI만 닫고, 모달·대사·로그·메뉴가 열리면 월드 입력을 캡처한다. 새 월드 객체/조건/액션은 `types.ts`, `scripts/build-world-map.mjs`(오버월드의 진실 소스), 맵 무결성 테스트, README/08/09를 함께 갱신한다. JSON 생성물만 직접 수정하지 않는다. 저장 구조를 바꾸면 스키마 버전·마이그레이션·저장 테스트도 함께 추가한다.

브라우저 수동 검증은 사용자가 명시적으로 요청할 때만 한다. 각 수정에는 재현을 막는 단위·맵 무결성·헤드리스 엔진 테스트를 추가하고, 끝에 `git status --short`, `pnpm typecheck && pnpm test`를 다시 실행한다. 테스트 실패는 해결하거나 원인과 범위를 문서화한다. 사용자 요청 전에는 커밋·푸시하지 않으며, 기존 사용자 파일과 미추적 파일(예: `.claude/settings.local.json`)은 절대 스테이징·삭제하지 않는다.
```

**사용자(병렬 가능)**
0. **S5 배포 확인**: 위의 "S5 배포 후 `?worldDebug` 확인 체크리스트"대로 확인하고 이상이 있으면 알려 주기. (S3·S2·S1 체크리스트의 남은 항목도 같은 방식.)
1. **난이도 튜닝**: 콘 코스 25초(깨끗한 주행이 약 5초)와 택배 90초(약 30초)는 너무 후하다. 플레이해 보고 `src/web/world/data/missionDefs.ts`의 `seconds`(콘은 `penalty`도), 킥·미니게임 임계값을 고치면 된다(코드 수정 없음).
2. **HUD 게이지 칸**: 프레임 그림의 둥근 칸 위에 조각이 어긋나 보이면 `world-mission.css`의 `--gauge-x`·`--gauge-pitch`·`--gauge-pip` 세 값 조정.
3. **실내 좌표 보정**(S2에서 이어짐): 실내 19곳의 충돌·조사 포인트는 눈대중이라 ±10px 어긋날 수 있다. `?worldDebug` 패널로 방을 열고 캔버스를 클릭해 나오는 px/tile 좌표로 `src/web/world/data/maps/interiors/<id>.json`을 고치면 된다(오버월드는 `overworld.json`이지만 **훈련장 펜스·골대·우편함·콘 코스·오브젝트는 `scripts/build-world-map.mjs`가 만든 값**이라 손으로 고치면 다시 생성할 때 사라진다 — 고칠 곳은 스크립트의 `OBJECTS`/`EXAMINE`/`forced`, 지면만 `--terrain-only`).
4. **이미지 톤·QA 확인**: [09 §10](09-asset-checklist.md#10-변환-결과와-qa-s1-2026-09-19)의 QA 목록에 [09 §11](09-asset-checklist.md#11-s2-소품-충돌-크기-대조-2026-09-19)의 "세로로 작게 그려진 소품"과 프레임 가운데 금 보석 늘어남을 더한 것을 보고, 다시 만들 것이 있으면 [10-image-generation-runbook.md](10-image-generation-runbook.md)의 해당 스텝만 재생성 후 `pnpm convert:world-art -- <카테고리> <이름>`.
5. **S5 난이도/시각 확인**: 위 S5 체크리스트로 러시 장애물 타이밍·게시판·도감을 확인하고 S6 튜닝 의견을 남긴다.
6. **배포 게이트**: 오디오 라이선스·누락 `region-weed`를 [07](07-audio.md)·[09](09-asset-checklist.md)에 정리한다. 이번 점검에서 내려받은 원본의 출처 메타데이터가 저장소에 없음을 확인했으므로, 파일명만으로 라이선스를 추정하지 않는다.

**기록용: 이전 S6 시작 지시문**

```text
다른 계정에서 이어받는 S6 세션이다. 작업 전 `docs/world/README.md`의 핸드오프 체크리스트·S1~S5 세션 로그·결정 로그를 모두 읽고, `docs/world/08-implementation-roadmap.md`의 S6 범위, `01-concept-and-architecture.md`의 입력/저장/오디오 규격, `07-audio.md`·`09-asset-checklist.md`의 파일명/에셋 상태를 읽어라. 현재 브랜치에는 S5까지의 미커밋 변경이 없고, S5 구현은 이미 커밋되어 있다고 가정한다. 시작 즉시 `git status --short`, `pnpm typecheck && pnpm test`를 실행해 기준 상태를 기록한다.

현재 완성 범위: S1~S5. 프롤로그·대사·결전·엔딩·제초동 게이트와 공장 입장(S4), 잔디 러시/5번 기계/랭크, KST 일일 게시판/스탬프, 황금 공 20개·도감·카드 도감·공장 정원 후속(S5)이 구현·테스트됐다. 이를 재구현하거나 저장 키/미션 ID/맵 객체 ID를 임의로 바꾸지 않는다. `WorldModals`의 월드 안 `onRoundEnd`/`onView`만 미니게임·카드 미션 판정에 쓰며, 대시보드에서 한 플레이/카드 열람은 월드 판정에 섞지 않는다. Esc는 최상단 UI만 닫고, 모달/대사/로그/메뉴가 열리면 월드 입력을 캡처한다. 이 우선순위를 유지한다.

S6 목표는 릴리스 품질이다. 다음을 실제 파일·코드 기준으로 점검하고 필요한 것만 구현한다.
1. 오디오: `worldAudio.ts`와 `07`의 표를 대조한다. 지구 BGM·실내/아케이드/보스/엔딩/러시, 9개 앰비언스, 지면별 발소리·SFX의 연결과 누락 파일 폴백을 검증한다. 앰비언스는 이미 9개 파일이 있고 현재 8개 선택 지점에 연결됐다. 새 호수 존을 만들지 않는 한 `water` 앰비언스 선택을 추가하거나 바꾸지 않는다. `region-weed` 누락 여부와 오디오 라이선스 기록을 해결할 때는 07·09도 함께 갱신한다.
2. 성능·접근성: 캔버스 렌더의 컬링/청크 캐시/메모리 증가 가능성을 코드와 테스트로 점검하고, 변경 전후 근거를 남긴다. `prefers-reduced-motion`에서 블룸·파티클·자동 애니메이션이 적절히 줄어드는지 확인한다. 키보드 포커스, 모달의 Esc/Tab, 색 대비, 스크린리더 라벨, 모바일에서 월드 토글 숨김을 회귀시키지 않는다. 브라우저 측정/수동 검증은 사용자 지시가 있을 때만 수행하고, 요청되지 않았다면 마지막에 `?worldDebug` 체크리스트만 작성한다.
3. 릴리스 마감: `world-credits`와 공지(`announcementsData.tsx`)를 기존 앱 구조에 맞춰 추가/정리하고, `docs/PROJECT_HANDOFF.md`, README, 08, 09를 실제 코드와 일치시킨다. 에셋 총량과 중복/미사용 대용량 파일을 점검하되 사용자 승인 없이 원본·에셋을 삭제하지 않는다. S5의 미사용 UI 스탬프/뱃지 그림 등은 시각 품질에 필요한 경우에만 연결하고, 새 파일이 필요하면 09와 해당 아트 문서도 갱신한다.
4. 버그 정리: 재현 가능한 문제만 고친다. 새 월드 객체/조건/액션은 `types.ts`, 맵 생성 소스(`scripts/build-world-map.mjs`가 진실인 오버월드 항목), 맵 무결성 테스트, README/08/09를 함께 변경한다. JSON 생성물만 직접 수정해 재생성 시 사라지게 하지 않는다. 저장 구조를 변경하면 스키마 버전·마이그레이션·저장 테스트를 함께 추가한다.

검증: 변경에 맞는 단위/맵 무결성/헤드리스 엔진 테스트를 추가한다. 시작과 끝에 `git status --short`, `pnpm typecheck && pnpm test`를 실행한다. 테스트 실패는 해결하거나 원인과 범위를 문서화하고 끝내지 않는다. 브라우저 검증은 하지 말라는 요청이 유지되면 하지 않는다. 마지막 응답에는 변경 요약, 통과 테스트 수, 잔여 위험, 그리고 `?worldDebug` 수동 확인 항목을 적는다.

Git: 사용자 요청 전에는 커밋·푸시하지 않는다. 이번 저장소의 기존 사용자 파일/미추적 파일(예: `.claude/settings.local.json`)은 절대 스테이징·삭제하지 않는다. 커밋 요청을 받았을 때도 본 세션이 만든 파일만 `git add`로 명시적으로 스테이징하고, `git diff --cached --check`와 최종 검증 후 하나의 명확한 커밋을 만든다.
```

## 용어집

| 용어 | 뜻 |
| --- | --- |
| 잔디 조각(shard) | 메인 미션 보상. 0~10, 맵 색 복원 단계와 1:1 |
| 황금 잔디 | 스타디움 중앙의 마을 심장. 결전 후 개화 |
| 제초동 / 제초왕 / 제초 코어 | 잔디를 밀어버리는 동호회 / 그 회장(가상 인물) / 결전 목표물 |
| 황금 축구공 | 숨은 수집품 20개(`gb-01`~`gb-20`) |
| 잔디 러시 | 신규 캔버스 러너 미니게임(오락실 5번 기계) |
| 마커 | NPC 머리 위 `?`(새 미션) / `!`(보고 가능) |
| 스테이지 | 논리 640×360 화면. 정수 배율로 확대 |
| P0/P1/P2 | 우선순위(필수 / 콘텐츠 완성 / 여력 시) |
| A1~A7 / S1~S6 | 아트 트랙 / 구현 세션 |

## 문서 갱신 규칙
- 규격·ID·파일명을 바꾸면 **01과 09를 먼저** 고치고 다른 문서를 맞춘다.
- 새 에셋이 필요해지면 09에 행을 추가하고 해당 프롬프트 문서(04~07)에 항목을 추가한다.
- 세션을 마칠 때 위 **핸드오프 체크리스트와 세션 로그**를 갱신한다.
- 결정이 바뀌면 결정 로그에 새 행을 추가(삭제하지 않고 "대체됨" 표기).
- 다른 문서 링크는 상대 경로, 코드 경로·라인은 백틱으로 표기(라인은 근사치이므로 구현 시 재확인).
